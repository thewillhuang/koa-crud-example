machine:
  services:
    - docker
    - postgresql
    - mysql
dependencies:
  override:
    - docker info
    - docker build -t circleci/wrenchsessions:$CIRCLE_SHA1 .

test:
  pre:
    - sudo bash -c "echo \"listen_addresses = '*'\" >>
      /etc/postgresql/9.4/main/postgresql.conf"
    - sudo bash -c "echo \"host all all 0.0.0.0/0 trust\" >>
      /etc/postgresql/9.4/main/pg_hba.conf"
    - sudo /etc/init.d/postgresql restart
  override:
    - docker run -d -e $PGCONNECTIONSTRING -e $HOST -e $USER -e $DATABASE circleci/wrenchsessions:$CIRCLE_SHA1 npm run test;
